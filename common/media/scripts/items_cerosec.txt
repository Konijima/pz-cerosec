module CeroSec
{
    /* The manual. Three items -- the three volumes of the set -- and every key
       on them verified against the game rather than against a wiki:

       - The block syntax and the key spellings are literature.txt's
         (media/scripts/generated/items/literature.txt): Base.Book at 1704,
         Base.Journal at 1777, Base.Notebook at 1817, Base.Book_Computer at
         1837. Build 42 has no `Type =` any more -- there is not one line of it
         left in media/scripts/generated/items -- it is `ItemType = base:<kind>`
         and the kinds are zombie.scripting.objects.ItemType's constants
         (javap: ALARM_CLOCK .. WEAPON_PART).

       - ItemType is base:normal and NOT base:literature, deliberately. A
         Literature item that cannot be written on is one the vanilla inventory
         menu offers to READ (ISInventoryPaneContextMenu.lua:242 sets
         isAllLiterature for it, :539 calls doLiteratureMenu, :1107 adds the
         Read option), and vanilla's Read is a timed action: the character sits
         down and burns hours of game time on it. This book is read by the
         player, not by the character -- so the item is a plain one and the only
         thing on its menu is ours.

       - DisplayCategory is a free string that the inventory prints through
         `getText("IGUI_ItemCat_" .. item:getDisplayCategory())`
         (ISInventoryPane.lua:2532-2533), and IGUI_ItemCat_Literature is a key
         the game ships (media/lua/shared/Translate/EN/IG_UI.json:276). So a
         Normal item still files itself under Literature, which is where a book
         belongs in the list.

       - Icon = CeroSecManualUser resolves to
         media/textures/Item_CeroSecManualUser.png:
         Item.DoParam builds `itemName = "Item_" + icon` and asks
         Texture.trygetTexture for it (javap -c zombie.scripting.objects.Item,
         the Icon branch at 528-567), and trygetTexture turns a bare name into
         "media/textures/<name>" and then appends ".png" (javap -v -c
         zombie.core.textures.Texture, bootstrap methods 7 and 8) before handing
         it to ZomboidFileSystem, which is what looks through the mod folders.

       - StaticModel and WorldStaticModel are Base.Book's own two, verbatim.
         Every literature item in the game defines both; without the world one
         there is no model to drop on the ground.

       DisplayName is here as the fallback; the name a player actually reads
       comes from Translate/EN/ItemName.json, keyed "CeroSec.ManualUser" the way
       vanilla keys "Base.Book_Computer". */

    /* Volume one of three. Same weight as the other two: they are the same
       book in three bindings, and a set whose volumes weigh differently in the
       pack is a set somebody would have to think about. */
    item ManualUser
    {
        DisplayName = CeroSec OS User's Guide,
        DisplayCategory = Literature,
        ItemType = base:normal,
        Weight = 0.8,
        Icon = CeroSecManualUser,
        StaticModel = Book,
        WorldStaticModel = BookClosedGround,
    }

    item ManualAdmin
    {
        DisplayName = CeroSec OS System Administrator's Guide,
        DisplayCategory = Literature,
        ItemType = base:normal,
        Weight = 0.8,
        Icon = CeroSecManualAdmin,
        StaticModel = Book,
        WorldStaticModel = BookClosedGround,
    }

    item ManualProgrammer
    {
        DisplayName = CeroSec OS Programmer's Guide,
        DisplayCategory = Literature,
        ItemType = base:normal,
        Weight = 0.8,
        Icon = CeroSecManualProgrammer,
        StaticModel = Book,
        WorldStaticModel = BookClosedGround,
    }

    /* RETIRED, AND STILL DECLARED. The single book that shipped before the set
       did.

       It is not loot -- CeroSecManualLoot names the three volumes above and
       nothing generates one any more -- and nothing this mod does will ever
       make another. It stays DEFINED because it is in SAVES, and the reason is
       worth writing down once, because it is the rule the whole mod now runs on
       (docs/CONTRIBUTING.md): an item script that stops naming an item does not
       retire it, it DELETES every copy of it in every container in the world.

       Traced through the jar (42.20.4), because "it probably still loads" is
       exactly the kind of thing that turns out to be false:

       - a saved item is a SHORT and not a name. InventoryItem.loadItem reads
         the registry id and calls InventoryItemFactory.CreateItem(short), which
         asks WorldDictionary.getItemInfoFromID and then ItemInfo.isValid()
         (javap -c zombie.inventory.InventoryItemFactory, CreateItem(S) at
         offsets 0-13).
       - a type no script declares any more resolves to nothing, CreateItem
         answers null, and loadItem skips the item's bytes, writes one line
         through DebugLog and returns null (javap -c
         zombie.inventory.InventoryItem, loadItem at 84-178).
       - CompressIdenticalItems.load then puts that null into the container's
         includingObsoleteItems and NOT into its items (javap -c
         zombie.inventory.CompressIdenticalItems, load at 114-140). The
         container is fine. The book is gone, and one line in the console is all
         anybody is told.

       AND THE `Obsolete` FLAG IS NOT THE WAY OUT. It is real -- Item.DoParam
       compares the key OBSOLETE case-insensitively and sets the field (javap -c
       zombie.scripting.objects.Item, offsets 10987-11007) -- and it does stop a
       type spawning: ItemContainer.AddItem(String) returns null for one (offsets
       50-58), ItemPickerJava logs `ignoring invalid ItemPicker item type "%s",
       obsolete = "%s"` and skips it, and RecipeManager leaves it out. But
       WorldDictionary.onLoadItem copies Item.getObsolete() into
       ItemInfo.obsolete (offsets 235-240), and DictionaryInfo.isValid() answers
       FALSE for an obsolete entry (offsets 0-26) -- which is the very test
       CreateItem(short) just failed above. So `Obsolete = true` DELETES every
       saved copy exactly as removing the block does. It is the flag for "this
       type is finished and so is everything made of it", not for "keep what
       people are holding". No vanilla item script sets it: there is not one
       `Obsolete` in media/scripts, and vanilla's Lua reads getObsolete() in one
       place, to hide an item from the admin item list
       (ISItemsListViewer.lua:53).

       So: DECLARED, out of every loot table, and converted into CeroSec.ManualUser
       -- the volume it became -- the first time a survivor reads it
       (CeroSecManualMenu.LEGACY). Every key below is the User's Guide's, because
       that is what it is; the icon is its own, so a copy still in a crate looks
       like the book it was printed as. */
    item Manual
    {
        DisplayName = CeroSec OS User's Manual,
        DisplayCategory = Literature,
        ItemType = base:normal,
        Weight = 0.8,
        Icon = CeroSecManual,
        StaticModel = Book,
        WorldStaticModel = BookClosedGround,
    }

    /* The floppy disks. Four of them, and the four are the same disk in four
       colours of shell -- which is how a box of them was sold in 1993 and why
       they all carry the same DisplayName: a survivor tells them apart by
       colour and by the label he writes on the state inside (`newfs`, and the
       drive's own listing), never by the name in his inventory.

       Weight is 0.1, set against the vanilla items it sits between rather than
       against a feeling: Base.Paperclip is 0.01, Base.BusinessCard 0.05,
       Base.Disc_Retail 0.3 and Base.Notebook 0.5. A 3.5-inch disk is a shell of
       stiff plastic around a sheet of mylar -- twenty grammes -- which is
       heavier than a card and lighter than a CD in its case.

       DisplayCategory is Electronics, an IGUI_ItemCat_ key the game ships
       (media/lua/shared/Translate/EN/IG_UI.json), for the same reason the
       manual files itself under Literature: the category is a free string the
       inventory prints through getText, and an unknown one prints the key.

       ItemType is base:normal and there is deliberately nothing else on these
       items -- no Tags, no MediaCategory. What a disk IS lives in its modData
       and is the OS's business; the item is the thing that carries it.

       Icon = CeroSecFloppyBlue resolves to
       media/textures/Item_CeroSecFloppyBlue.png, and WorldStaticModel =
       CeroSecFloppyBlue to the model block in models_cerosec.txt. Both names
       carry the mod's own prefix so that a world with the old Computer mod
       installed beside this one has nothing to collide over.

       The name a player reads comes from Translate/EN/ItemName.json, keyed
       "CeroSec.FloppyBlue"; the DisplayName here is the fallback, and it is
       spelled out in words because a double quote in a script value is not
       something to bet on. */
    /* THE PAPER SOMEBODY WROTE A PASSWORD ON.
       One item for the two places a password is found -- the drawer of a desk and
       the pocket of a dead employee -- because it IS one thing: a note with a
       login and a password on it. Which one it is is in the name the item carries
       (CeroSecNotes writes it with setName), and nothing else about the item
       differs.

       base:normal and not base:literature, for the manuals' own reason: a
       Literature item that cannot be written on is one vanilla's inventory menu
       offers to READ, and Read is a timed action that burns hours of game time.
       There is nothing to read here that the item's name does not already say.

       StaticModel and WorldStaticModel are SheetPaper2's own, verbatim
       (media/scripts/generated/items/literature.txt:1839-1840): a sheet of paper
       on the floor, which is what this is.

       Weight is 0.01 -- vanilla's lightest paper is 0.1 and that is a whole sheet.
       A survivor must never leave a password behind because his bag was full.

       DisplayName here is the fallback; the name a player reads comes from
       Translate/EN/ItemName.json, and the name he reads on a note that has been
       WRITTEN on is neither: it is the custom name CeroSecNotes puts on it. */
    item StickyNote
    {
        DisplayName = Sticky Note,
        DisplayCategory = Literature,
        ItemType = base:normal,
        Weight = 0.01,
        Icon = CeroSecStickyNote,
        StaticModel = SheetOfPaper,
        WorldStaticModel = SheetOfPaper,
    }

    /* THE FOUR DISKS, and the one key on them that is not decoration.

       OnCreate names a Lua function that is called with the item as its ONE
       argument the moment the item is made, and it is how a floppy off a shelf
       sometimes has something written on it. Traced through the jar (42.20.4),
       because an item-script key is exactly the kind of thing that turns out not
       to work:

       - Item.DoParam reads the key "OnCreate" into the private field luaCreate
         (javap -c zombie.scripting.objects.Item: ldc_w "OnCreate" at 11017,
         putfield luaCreate at 11031).
       - Item.InstanceItem(String, boolean) -- which every item creation goes
         through -- calls InventoryItem.initialiseItem() at offset 4059, guarded
         by isInitialised() and by the calling thread being GameWindow.gameThread,
         GameLoadingState.loader or GameServer.mainThread (4022-4055). Loot
         generation runs on the loader thread.
       - InventoryItem.initialiseItem() looks the name up with
         LuaManager.getFunctionObject and calls it through
         LuaCaller.protectedCallVoid with the item as the only argument (offsets
         6-32).
       - getFunctionObject splits a DOTTED name on "." , walks each segment out of
         the global environment with rawget and requires each intermediate to be a
         KahluaTable (offsets 31-124) -- which a plain Lua table of ours is. So
         `CeroSecContent.onCreateFloppy` resolves.

       And vanilla ships the key itself: media/scripts/generated/items/
       literature.txt:4795 is `OnCreate = ItemCodeOnCreate.onCreatePaperwork` on
       Base.Paperwork. */
    item FloppyBlue
    {
        DisplayName = 3.5 inch Floppy Disk,
        DisplayCategory = Electronics,
        ItemType = base:normal,
        Weight = 0.1,
        Icon = CeroSecFloppyBlue,
        WorldStaticModel = CeroSecFloppyBlue,
        OnCreate = CeroSecContent.onCreateFloppy,
    }

    item FloppyYellow
    {
        DisplayName = 3.5 inch Floppy Disk,
        DisplayCategory = Electronics,
        ItemType = base:normal,
        Weight = 0.1,
        Icon = CeroSecFloppyYellow,
        WorldStaticModel = CeroSecFloppyYellow,
        OnCreate = CeroSecContent.onCreateFloppy,
    }

    item FloppyRed
    {
        DisplayName = 3.5 inch Floppy Disk,
        DisplayCategory = Electronics,
        ItemType = base:normal,
        Weight = 0.1,
        Icon = CeroSecFloppyRed,
        WorldStaticModel = CeroSecFloppyRed,
        OnCreate = CeroSecContent.onCreateFloppy,
    }

    item FloppyGreen
    {
        DisplayName = 3.5 inch Floppy Disk,
        DisplayCategory = Electronics,
        ItemType = base:normal,
        Weight = 0.1,
        Icon = CeroSecFloppyGreen,
        WorldStaticModel = CeroSecFloppyGreen,
        OnCreate = CeroSecContent.onCreateFloppy,
    }

    /* The hardware modules, rung 4f.

       Four boxes a survivor screws to a door, a window or a light switch so
       that a computer can reach it (CeroSecModules.lua, and the manual's
       chapter on /dev). 1993 vocabulary and nothing invented: an electric
       strike, a door operator, a magnetic contact and a relay are what the
       trade called these four things then and what it calls them now.

       Same keys as the disks above and for the same reasons -- ItemType is
       base:normal, DisplayCategory is the Electronics the game already ships a
       name for, the DisplayName here is the fallback and Translate/EN's
       ItemName.json is what a player reads.

       WorldStaticModel names a VANILLA model in every case, the way the manual
       items name Book and BookClosedGround: this mod ships no mesh for these
       and a made-up name is an item that drops on the floor as nothing at all.
       MotionSensor for the contact (it is a sensor in a box), ElectronicsScrap
       for the relay (a board), ScrapMetal for the strike and the operator (a
       lump of metal is what either looks like lying on a road).

       The weights are set against the vanilla items they sit between rather
       than against a feeling: Base.ElectronicsScrap is 0.1,
       Base.MotionSensor 0.3, Base.SmallSheetMetal 0.5, Base.Doorknob 1.0,
       Base.HomeAlarm 1.0 and Base.SheetMetal 2.0. So: a contact is two reed
       switches in plastic, a relay is a board in a case, a strike is a door
       fitting the size of a knob, and an operator is a motor and an arm -- the
       heaviest thing in this mod and the only one a survivor thinks twice
       about carrying. */

    item MagneticContact
    {
        DisplayName = Magnetic Contact,
        DisplayCategory = Electronics,
        ItemType = base:normal,
        Weight = 0.2,
        Icon = CeroSecMagneticContact,
        WorldStaticModel = MotionSensor,
    }

    item Relay
    {
        DisplayName = Relay Module,
        DisplayCategory = Electronics,
        ItemType = base:normal,
        Weight = 0.3,
        Icon = CeroSecRelay,
        WorldStaticModel = ElectronicsScrap,
    }

    item ElectricStrike
    {
        DisplayName = Electric Strike,
        DisplayCategory = Electronics,
        ItemType = base:normal,
        Weight = 1.0,
        Icon = CeroSecElectricStrike,
        WorldStaticModel = ScrapMetal,
    }

    item DoorOperator
    {
        DisplayName = Door Operator,
        DisplayCategory = Electronics,
        ItemType = base:normal,
        Weight = 2.0,
        Icon = CeroSecDoorOperator,
        WorldStaticModel = ScrapMetal,
    }

    /* THE FOUR THE MOTOR RUNG ADDED, and the same keys as the first four for
       the same reasons: ItemType is base:normal, DisplayCategory is the
       Electronics the game already ships a name for, the DisplayName here is the
       fallback and Translate/EN's ItemName.json is what a player reads, and the
       WorldStaticModel is a VANILLA model because this mod ships no mesh and a
       made-up name is an item that lies on the floor as nothing at all.

       The weights are set the way the first four were -- against the vanilla
       items they sit between and not against a feeling. Base.ElectronicsScrap is
       0.1, Base.MotionSensor 0.3, Base.SmallSheetMetal 0.5, Base.Doorknob 1.0,
       Base.HomeAlarm 1.0, Base.SheetMetal 2.0, and this mod's own door operator
       is 2.0 because it is a motor, an arm and a limit switch.

         a curtain motor is a tube and a crown that live inside the rail: a
         small motor with no arm on it at all, so 1.0
         a window operator is the same motor with a gearbox and a scissor arm,
         and it moves a sash and not a door -- 1.5, under the door operator
         a contactor is a board and a coil in a case, which is the relay's
         weight and a little more for the poles -- 0.5
         a transfer switch is a steel box with a lever a survivor puts his whole
         hand on -- 1.0

       Tooltips on all four, which the first four have not got. They earn one
       because what each of them FITS is not obvious from its name: a survivor
       reading "Appliance Switch" has no way to know it goes on an oven and on a
       washing machine and on nothing else. */

    item CurtainMotor
    {
        DisplayName = Curtain Motor,
        DisplayCategory = Electronics,
        ItemType = base:normal,
        Weight = 1.0,
        Icon = CeroSecCurtainMotor,
        Tooltip = Tooltip_item_CeroSecCurtainMotor,
        WorldStaticModel = ScrapMetal,
    }

    item WindowOperator
    {
        DisplayName = Window Operator,
        DisplayCategory = Electronics,
        ItemType = base:normal,
        Weight = 1.5,
        Icon = CeroSecWindowOperator,
        Tooltip = Tooltip_item_CeroSecWindowOperator,
        WorldStaticModel = ScrapMetal,
    }

    item ApplianceSwitch
    {
        DisplayName = Appliance Switch,
        DisplayCategory = Electronics,
        ItemType = base:normal,
        Weight = 0.5,
        Icon = CeroSecApplianceSwitch,
        Tooltip = Tooltip_item_CeroSecApplianceSwitch,
        WorldStaticModel = ElectronicsScrap,
    }

    item GeneratorSwitch
    {
        DisplayName = Generator Switch,
        DisplayCategory = Electronics,
        ItemType = base:normal,
        Weight = 1.0,
        Icon = CeroSecGeneratorSwitch,
        Tooltip = Tooltip_item_CeroSecGeneratorSwitch,
        WorldStaticModel = ScrapMetal,
    }

    /* The tuner control: the box that switches a television or a radio set on and
       moves its dial. A board and a ribbon and nothing that turns, so it is the
       appliance switch's weight and the appliance switch's world model -- a board
       on the ground is Base.ElectronicsScrap' model, which is what this is. */
    item TunerControl
    {
        DisplayName = Tuner Control,
        DisplayCategory = Electronics,
        ItemType = base:normal,
        Weight = 0.5,
        Icon = CeroSecTunerControl,
        Tooltip = Tooltip_item_CeroSecTunerControl,
        WorldStaticModel = ElectronicsScrap,
    }

    /* THE SMALL MOTOR, and why the game needed one.

       Every module that MOVES something is built around a motor, and Build 42
       has none: a `grep -rni motor` over the whole of media/scripts returns ten
       hits and every one of them is a motorcycle helmet, a pair of motorcycle
       boots or the Louisville Motor Shop step van (docs/notes/actuators.md,
       "The motor, from vanilla data alone"). There is no Base.Motor, no
       Base.SmallMotor and no Base.ElectricMotor to borrow.

       So it is declared here, and it is NOT a duplicate of the two vanilla
       parts nearest to it: Base.EngineParts is a car part and stays where it
       is on the door operator, and Base.ElectronicsScrap is a board. This is
       the thing with a shaft on it.

       Where one comes from is a recipe and not a shelf -- there are four items
       in this game that really have a motor in them and a survivor takes a
       screwdriver to any of them (recipes_cerosec.txt). It is on the same nine
       shelves the modules are (CeroSecModuleLoot) at half the rarest module's
       share, because those shelves held finished stock and a loose motor in a
       crate is something that fell off a bench.

       The keys are the four modules' own, with one addition. MetalValue = 8.0
       is Base.HairDryer's whole metal value (normal.txt:2787), and the motor is
       where nearly all of it is: a survivor who smelts one gets what he would
       have got for the dryer. Base.MotionSensor and Base.Receiver -- the two
       vanilla part items this one is shaped on -- both carry MetalValue = 7.0
       beside a Tooltip (normal.txt:4539, :4574), so the pair is vanilla's shape
       for a part and not an invention.

       WorldStaticModel = ScrapMetal, the strike's and the operator's own: this
       mod ships no mesh, a made-up name is an item that lies on the floor as
       nothing at all, and a lump of metal is what a motor looks like on a road.

       DisplayName is the fallback; ItemName.json is what a player reads. */

    item SmallMotor
    {
        DisplayName = Small Motor,
        DisplayCategory = Electronics,
        ItemType = base:normal,
        Weight = 0.3,
        Icon = CeroSecSmallMotor,
        MetalValue = 8.0,
        Tooltip = Tooltip_item_CeroSecSmallMotor,
        WorldStaticModel = ScrapMetal,
    }

    /* The book the four recipes come out of.

       This is the one item in the mod that IS base:literature, and it is the
       opposite of the decision the manual volumes took at the top of this file.
       They are base:normal precisely so that vanilla adds no Read of its own,
       because they are read by the player. This one is read by the CHARACTER:
       vanilla's Read is the whole mechanism, and every line of it is the
       engine's.

       - `LearnedRecipes` is the B42 spelling. There is no `TeachedRecipes` left
         anywhere in media/scripts -- not one line -- and the key the game reads
         is this one: literature.txt:5307-5320 (Base.ElectronicsMag1, which
         teaches the remote controller) and :5781-5786 (Base.MechanicMag1).

       - Reading it teaches, and no Lua of ours is involved at any point.
         IsoGameCharacter.ReadLiterature(Literature) walks
         Literature.getLearnedRecipes(), skips what getKnownRecipes() already
         contains and calls learnRecipe(String) on the rest -- javap -c
         zombie.characters.IsoGameCharacter, the ReadLiterature body at
         offsets 24-81. The Lua side only offers the option
         (ISInventoryPaneContextMenu.lua:1081 spots an item with
         getLearnedRecipes(), :1107 adds Read) and runs the timed action
         (ISReadABook, whose perform() at :194 does not learn anything: it
         closes the book).

       - Every other key on it is Base.ElectronicsMag1's, verbatim, because a
         magazine that weighs or files itself differently from the five the game
         already prints is a magazine a player can tell is a mod's:
         DisplayCategory = RecipeResource (IGUI_ItemCat_RecipeResource is a key
         the game ships, EN/IG_UI.json:284), Weight = 0.5, BoredomChange -20 and
         StressChange -15, Tags = base:magazine.

       - StaticModel = Magazine and WorldStaticModel = MagazineGround are
         Base.SmithingMag1's own two (literature.txt:12-13) and both are
         declared in vanilla: models_items.txt:281 and :4493. The five
         ElectronicsMag items each name a per-issue ground model of their own
         (MagazineElec1Ground and its four brothers) and this book is not one of
         their issues, so it takes the generic pair rather than borrowing an
         issue's cover.

       - OnCreate is vanilla's own and it does exactly one thing: javap -c
         zombie.scripting.logic.ItemCodeOnCreate.onCreateRecipeMagazine is four
         instructions long and they set modData.literatureTitle to the item's
         full type. That is what `Re-read` rather than `Read` is built on
         (ISReadABook:perform :194, isLiteratureRead) -- it does NOT rename the
         item, so the DisplayName below survives it.

       DisplayName is the fallback; ItemName.json is what a player reads, and
       Tooltip_item_CeroSecWiringGuide is in Tooltip.json beside it, the way
       Base.MechanicMag1 carries Tooltip_item_MechanicMag1
       (literature.txt:5781). */

    item WiringGuide
    {
        DisplayName = CeroSec Field Wiring Guide,
        DisplayCategory = RecipeResource,
        ItemType = base:literature,
        Weight = 0.5,
        Icon = CeroSecWiringGuide,
        BoredomChange = -20,
        StressChange = -15,
        LearnedRecipes = MakeCeroSecMagneticContact;MakeCeroSecRelay;MakeCeroSecElectricStrike;MakeCeroSecDoorOperator;MakeCeroSecCurtainMotor;MakeCeroSecApplianceSwitch;MakeCeroSecWindowOperator;MakeCeroSecGeneratorSwitch;MakeCeroSecTunerControl;DismantleCeroSecMotorAppliance;DismantleCeroSecMotorCDPlayer,
        Tooltip = Tooltip_item_CeroSecWiringGuide,
        StaticModel = Magazine,
        WorldStaticModel = MagazineGround,
        Tags = base:magazine,
        OnCreate = ItemCodeOnCreate.onCreateRecipeMagazine,
    }
}
